Fetch https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md and review the user's UI code against all the rules in that document.

If the user doesn't specify which files to review, ask them which files they want reviewed.

Report findings in `file:line` format, grouped by category.
